<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="jt.projects.gbweatherapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_CONTACTS" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" /> <!-- GPS -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:name=".MyApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/ic_earth"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_earth"
        android:supportsRtl="true"
        android:theme="@style/Theme.DarkTheme"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">


        <!--
             TODO: Before you run your application, you need a Google Maps API key.

             To get one, follow the directions here:

                https://developers.google.com/maps/documentation/android-sdk/get-api-key

             Once you have your API key (it starts with "AIza"), define a new property in your
             project's local.properties file (e.g. MAPS_API_KEY=Aiza...), and replace the
             "YOUR_API_KEY" string in this file with "${MAPS_API_KEY}".
             AIzaSyA5TR_w2YguV0jQXbk9fQttQTkhSNbtfLA
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyC4Q7OtEFPeHIh5GARicSvXNTMEJ7g0Dfk" />


        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- > Сервисы д.б. в манифесте < -->
        <service android:name="jt.projects.gbweatherapp.memo.ExService" />
        <service android:name="jt.projects.gbweatherapp.memo.ExIntentService" />
        <service android:name="jt.projects.gbweatherapp.utils.old.NetworkStatusService" />
        <service android:name="jt.projects.gbweatherapp.utils.old.NetworkStatusService" /> <!-- > BroadcastReceiver для приема сообщений извне д.б. в манифесте < -->
        <!-- > Контент-провайдер д.б. в манифесте < -->
        <provider
            android:name="jt.projects.gbweatherapp.memo.GBContentProvider"
            android:authorities="@string/authorities"
            android:enabled="true"
            android:exported="true" /> <!-- > Broadcast Reciever д.б. в манифесте < -->
        <receiver
            android:name="jt.projects.gbweatherapp.memo.ExBroadcastReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.AIRPLANE_MODE" />
            </intent-filter>
        </receiver>
    </application>

</manifest>